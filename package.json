{
  "name": "@pattieboom/shopify-core",
  "version": "1.0.10",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "clean": "rimraf dist",
    "build": "npm run clean && tsup",
    "release:patch": "git add . && git commit -m \"release\" || echo \"no changes\" && npm run build && npm version patch && npm publish",
    "release:minor": "git add . && git commit -m \"release\" || echo \"no changes\" && npm run build && npm version minor && npm publish",
    "release:major": "git add . && git commit -m \"release\" || echo \"no changes\" && npm run build && npm version major && npm publish"
  },
  "devDependencies": {
    "rimraf": "^6.1.3",
    "tsup": "^8.0.0",
    "typescript": "^5.4.0"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@shopify/admin-api-client": "^1.1.2"
  }
}
