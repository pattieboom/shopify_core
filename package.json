{
  "name": "@pattieboom/shopify-core",
  "version": "1.0.7",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
"release:patch": "git add . && git commit -m \"release\" || echo \"no changes\" && npm version patch && npm publish",
    "release:minor": "git add . && git commit -m \"release\" || echo \"no changes\" && npm version minor && npm publish",
    "release:major": "git add . && git commit -m \"release\" || echo \"no changes\" && npm version major && npm publish"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.4.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
